TUI to browse random crates on docs.rs/crates.io

NOTE: this currently requires a nightly compiler to build with the `impl_trait_in_assoc_type` feature to avoid an allocation when creating a connection pool instance
